# Fix Log — 2026-08-15 — تسليم لأي AI تاني (Claude Code, GPT, Cursor, إلخ)

انسخ الملف ده وابعته كأول رسالة لأي AI هيكمل الشغل. الهدف: يعرف بالظبط إيه اللي **اتنفذ فعليًا على الكود** (مش تقرير نظري) وإيه اللي لسه باقي، عشان يكمل من غير ما يعيد فحص أو يكرر شغل.

المصدر: تم الفحص والتنفيذ على الريبو `https://github.com/omotaz556-cloud/tatar.git`، وتحديدًا على الفولدر الحي الفعلي `tatar_work/tatar_clean/` (ده المشروع الشغال — باقي الملفات في جذر الريبو كانت أرشيفات zip بس).

---

## ⚠️ ملاحظة بنيوية مهمة قبل أي حاجة

الريبو الأصلي بنيته فوضوية: الكود الحي مش في جذر الريبو مباشرة، لكنه متحط جوه `tatar_work/tatar_clean/`، والجذر نفسه كان فيه بس ملفين zip كبار (37MB + 48MB). الـ zip المُسلَّم في التسليم ده هو محتوى `tatar_clean/` بس (المشروع الفعلي الشغال)، مش الريبو بالكامل بهيكله الأصلي. **قرار مطلوب من صاحب المشروع:** هل يتنقل `tatar_clean/` ليبقى هو جذر الريبو نفسه؟ ده تحسين بنيوي مش أولوية قانونية، فمتعملش من غير موافقة صريحة لأنه ممكن يكسر أي CI/deploy script بيفترض المسار الحالي.

---

## ✅ اللي اتنفذ فعليًا (كود حقيقي اتعدل، مش توصية)

### 🔴 البند الحرج #1 — الأرشيف الملوّث بترخيص CC BY-NC-SA — **تم الحل بالكامل**

- اتأكد فعليًا (بفتح الـ zip وفحص محتواه) إن `novaterra_rebrand_wip.zip` (37MB) في جذر الريبو كان لسه فيه نسخة كاملة من `notification/gpack/notification_v1_zzjhons/` — كود ZravianX/ZZJHONS المرخّص **CC BY-NC-SA 3.0** (غير تجاري)، بترويسة "YOU MUST NOT REMOVE OR CHANGE THIS NOTICE".
- الفولدر الحي `notification/` كان بالفعل نضيف (صفحة Coming Soon أصلية بس) — المشكلة كانت **فقط** في الأرشيف القديم المنسي في الريبو.
- **الإجراء:** الأرشيف اتحذف بالكامل (`git rm` + حذف من القرص).
- اتفحص أرشيف تاني موجود (`tatar_work/novaterra_clean.zip`، 48MB) وتأكد إنه **نضيف 100%** — مفيهوش أي أثر لـ zzjhons.
- **إجراء وقائي:** اتعمل `.gitignore` جديد (كان مش موجود أصلاً من قبل — ده السبب الجذري للمشكلة) بيمنع دخول أي `.zip/.tar/.rar/.7z` للريبو تاني، وبيحجب `tatar_work/`، `GameEngine/config.php`، و`var/installed` من التتبع.

**الحالة: مغلق. لا يوجد أي أثر لكود CC BY-NC-SA في المشروع دلوقتي.**

---

### 🔴 البند الحرج #2 — تناقض التراخيص في ترويسات الملفات — **تم الحل، بتصنيف دقيق**

**اكتشاف مهم غيّر خطة التنفيذ:** الوصف الأصلي في التقرير افترض إن كل الـ 150+/362 ملف لازم نفس المعالجة. الفحص الفعلي كشف الصورة أدق:

- من أصل **361 ملف** بترويسة "License: Novaterra Project — Copyright: Novaterra (c) 2010-2026. All rights reserved":
  - **359 ملف** = كود **مشتق من TravianZ الأصلي** (زي `index.php`, `GoldShop.php`, معظم `GameEngine/`, `Admin/`, `Templates/`) — الترويسة الملكية دي **غلط قانونيًا** لأنه كود GPL.
  - **2 ملف بس** = كود **جديد بالكامل** كُتب من الصفر لميزة الذهب المركزي (`GameEngine/CentralGold.php` و`GameEngine/Admin/Mods/centralGoldAdmin.php`) — الترويسة الملكية دي **صح فعليًا** ليهم، لأنهم مش مشتقين من أي كود TravianZ أصلي.

**الإجراء المُنفَّذ:**
- الـ 359 ملف: سطر `License:` اتغيّر لـ `GPLv3 (derived from TravianZ; see project LICENSE)`، وسطر `Copyright:` اتغيّر ليوضّح الفرق بين تعديلات Novaterra والمحرك الأصلي (GPLv3، ملك المؤلفين الأصليين).
- الـ 2 ملف الجديد: **اتسابوا زي ما هما بالظبط** — مفيش أي تعديل عليهم، لأن ادعاءهم الحصري صحيح.
- باقي محتوى الترويسة (Filename, Type, Developed by, Contact القديم قبل التنظيف, إلخ) **اتسابت زي ما هي** — التعديل كان على سطرين بس (License + Copyright)، مش إعادة كتابة الترويسة بالكامل.

**ملاحظة تجميلية بسيطة (غير قانونية):** سطر الـ Copyright الجديد أطول شوية من عرض الـ 78-82 حرف المعتاد للترويسة، فمحاذاة `##` على اليمين مش دقيقة 100% بصريًا في بعض الملفات. القيمة القانونية سليمة، بس لو حد عايز يظبط التنسيق بصريًا لاحقًا ينفع.

**الحالة: مغلق قانونيًا. تحسين تجميلي اختياري متبقي (غير عاجل).**

---

### 🔴 البند الحرج #3 — `GameEngine/config.php` فيه أسرار حقيقية — **تم الحل بالكامل**

- اتأكد فعليًا: `CRON_KEY` حقيقي مولّد (`8e9ce6c862c1a18301a55534ea64937a7d38d2a8b1c1e028`)، و`ADMIN_EMAIL` ببريد شخصي حقيقي (`omotaz323@gmail.com`) — الاتنين كانوا في ملف **متتبع في git** فعليًا.
- اتأكد إن `install/process.php` هو اللي بيولّد `config.php` ديناميكيًا وقت التثبيت (بما فيه توليد `CRON_KEY` عشوائي) — يعني الملف الحالي كان مجرد خرج تجريبي من تشغيل سابق للـ installer، مش حاجة لازم تتحفظ في الريبو من الأساس.

**الإجراء المُنفَّذ:**
- اتعملت نسخة `GameEngine/config.php.example` نظيفة — بـ `CRON_KEY` = placeholder واضح، و`ADMIN_EMAIL` = `admin@example.com`، ومعاها تعليق أعلى الملف يوضّح إنه template وإن الملف الحقيقي بيتولّد أوتوماتيك.
- `GameEngine/config.php` الحي (بالأسرار الحقيقية) اتشال من تتبع git (`git rm --cached`) — **الملف نفسه لسه موجود على القرص محليًا** عشان المشروع يفضل شغال، لكن مش هيرجع للريبو تاني بفضل `.gitignore`.
- **مهم:** ملف `config.php` الحي **متضمنش في الـ zip المُسلَّم ده** عمدًا (بيحتوي سر حقيقي) — بس `config.php.example` النظيف موجود.

**الحالة: مغلق. أي AI/مطوّر جديد لازم ينسخ `config.php.example` إلى `config.php` ويشغّل معالج التثبيت، أو يولّد `CRON_KEY` جديد عشوائي بنفسه.**

---

### 🆕 بند إضافي اتكتشف أثناء التنفيذ (مرتبط بالبند المتوسط #10 الأصلي) — **جزئي، محتاج إكمال**

أثناء تنظيف ترويسات الملفات، اتكشف إن بريد شخصي حقيقي لمطوّر TravianZ الأصلي (`cata7007@gmail.com`) مش بس موجود في تعليقات الترويسة زي ما التقرير الأصلي وصف — لكن كان كمان **fallback فعلي شغال في كود حي** بيأثر على المستخدمين الحقيقيين:

```php
// في a2b2.php — صفحة كشف حساب الذهب الحقيقية اللي بيشوفها العملاء
<a href="mailto:<?php echo defined('PAYPAL_EMAIL') ? PAYPAL_EMAIL : 'cata7007@gmail.com'; ?>">our billing address</a>
```

لو `PAYPAL_EMAIL` مش متعرّف (سيناريو وارد جدًا في نشر جديد لسه مالوش كل الإعدادات)، أي عميل حقيقي بيواجه مشكلة دفع كان هيتوجّه لبريد شخص غريب تمامًا — ده مش بس تسريب بيانات، ده **خطر تجاري حقيقي** لصاحب المشروع.

**الإجراء المُنفَّذ:**
- `a2b2.php`: الـ fallback اتغيّر من الإيميل الشخصي الثابت لـ `ADMIN_EMAIL` (constant دايمًا متعرّف عن طريق نفس الـ installer).
- **81 ملف `.php`** فيهم الإيميل في تعليقات الترويسة (`Contact:`, `Fixed by:`, تعليقات كود) — اتنضفوا بالكامل.

**المتبقي (لسه محتاج تنفيذ):**
- **289 ملف `.tpl`** (Templates) فيهم نفس نمط الإيميل في الترويسة — **لسه ماتلمسوش**. اكتُشفوا في آخر خطوة قبل ما وصلنا لحد أقصى أدوات الجلسة.
- `version.php` سطر 201: فيه رابط حي (مش تعليق) `https://paypal.me/cata7007` — **قرار مطلوب من صاحب المشروع**: يتشال، ولا يتحط مكانه رابط دفع/تبرع خاص بالعميل؟
- `CODE_OF_CONDUCT.md` سطر 37: بيقول إن شكاوى سوء السلوك تتبعت لنفس الإيميل الشخصي — سهل الاستبدال ببريد فريق حقيقي أو يتشال السطر لو مفيش قناة إبلاغ رسمية لسه.

---

## 🔴 لسه لم يُلمس إطلاقًا (البنود الحرجة العالية الأولوية والمتوسطة من التقرير الأصلي)

### عالية الأولوية
4. **صور فن الأبطال تشبه Travian الرسمي** (`img/un/h/gallier.jpg`, `roemer.jpg`, `germane.jpg`, `img/reg/portrait_v1-v9.png`) — لسه محتاجة فحص بصري + قرار (استبدال بفن أصلي / ترخيص / مراجعة قانونية متخصصة). لم يُفتح أو يُقارن أي ملف صورة في الجلسة دي.
5. **شعارات PayPal/Skrill الحقيقية** في `img/bezahlung/` — لسه موجودة، لم تُفحص أو تُستبدل.
6. `AC_OETags.js` (كود Adobe/Macromedia محمي، غير مستخدم) — التقرير الأصلي قال إنه قابل للحذف الآمن الفوري، **لكن ده لم يتأكد فعليًا في الجلسة دي** — لازم `grep` شامل يتأكد إنه بجد مش متضمن في أي `<script src>` قبل الحذف.
7. **كلمة مرور Multihunter الافتراضية `123456`** بعد Server Reset (`Admin/Templates/resetdone.php`) — لم تُفحص أو تُصلح.
8. **`docker-compose.yml`**: منفذ DB 3306 مكشوف + phpMyAdmin بكلمة مرور افتراضية — لم يُفحص أو يُعدَّل.

### متوسطة الأولوية
9. ملفات بأسماء فيها "zravianx" (`footer_logo_zravianx.gif`, `banner-zravianx.png`) — لم تُعاد تسميتها.
11. رابط GitHub placeholder `https://github.com/YOUR-ORG/Novaterra` — **لاحظنا وجوده في عدة ملفات أثناء الفحص** (زي `GoldShop.php`, `a2b2.php`) بس لم يُستبدل — محتاج الرابط الحقيقي من صاحب المشروع الأول.
12. فولدر `installed_1786718416/` — لم يُفحص الغرض منه.
13. `var/installed` — **اتفحص في نسخة التسليم ده ومكانش موجود أصلاً**، لكن لو ظهر في نسخة تانية من المشروع (كان فيه إشارة له في التقرير الأصلي) لازم يتحذف قبل أي تثبيت جديد.
14. ~380 صورة خارج `gpack/` لسه محتاجة فحص بصري كامل.
15. `img/lol.PNG` مستخدمة فعليًا في `maintenance.php` بدل `img/maintenance.png` الأجود — لم يُصلح.

---

## قواعد التنفيذ اللي اتبعت (كمّل عليها)

- كل تعديل كان **تعديل نصي دقيق** (سطر أو سطرين بالظبط) مش إعادة كتابة ملفات كاملة — عشان نضمن مفيش أي كسر لوظائف تانية.
- الفرق بين **الطبقة المشتقة من GPL** (محرك اللعبة، 359 ملف) و**الطبقة الحصرية الحقيقية** (CentralGold، 2 ملف) اتوضّح عمليًا مش نظريًا بس — كل نوع اتعامل معاه بشكل مختلف فعلاً في الكود.
- أي حاجة مش متأكد منها متفترضتش — زي رابط GitHub الحقيقي أو قرار استبدال رابط PayPal الشخصي، دي محتاجة تأكيد من صاحب المشروع مش افتراض من الـ AI.
- الملف الحي بالأسرار الحقيقية (`config.php`) **متضمنش في أي تسليم** — بس النسخة النظيفة `.example`.

---

---

## ✅ تحديث — الدفعة الثانية من التعديلات (نفس الجلسة، استكمال مباشر)

### إكمال بند الإيميل الشخصي (`cata7007@gmail.com`) — **مغلق بالكامل الآن**

- **289 ملف `.tpl`** كانت متبقية من الدفعة الأولى — اتنضفت كلها (تعليقات الترويسة `Contact:`).
- اتكشف نمط تاني أثناء الفحص: **`mailto:cata7007@gmail.com` حي فعليًا** (مش تعليق) في `install/templates/end.tpl` و`installed_1786718416/templates/end.tpl` — دي صفحة "اكتمل التثبيت" اللي بتظهر لأي عميل جديد بيثبّت السيرفر، وفيها زرار "Contact Support" كان بيوجّه لبريد المطوّر الأصلي الشخصي. اتصلح ليشاور على `ADMIN_EMAIL` (نفس الـ constant اللي الـ installer نفسه بيطلب من العميل يدخله).
- `Templates/Profile/medal.php`: شيلت Skype handle شخصي (`cata7007`) من تعليق ترويسة.
- `CODE_OF_CONDUCT.md`: قناة الإبلاغ عن سوء السلوك كانت بتوجّه لنفس الإيميل الشخصي — استبدلتها بصياغة عامة ("القنوات الرسمية للمشروع") لحد ما يتحدد بريد فريق حقيقي.
- **قرار مقصود بعدم التعديل:** `version.php` لسه فيه رابط `paypal.me/cata7007` — ده كارت "شكر وتقدير" (Credits) لفريق TravianZ الأصلي في صفحة عامة، مش سلوك مضلل للعملاء زي حالة `a2b2.php`. اعتبرته قرار تحريري/تجاري لصاحب المشروع (يفضل الكريدت والرابط ولا يتشال)، مش مخالفة قانونية تستدعي تدخل تلقائي.

**الحالة: كل الاستخدامات الوظيفية الحقيقية للإيميل الشخصي اتصلحت. الباقي الوحيد قرار بشري صريح.**

---

### 🟠 البند العالي #7 — كلمة مرور Multihunter الافتراضية `123456` — **تم الحل**

اكتشاف مهم: الكود الفعلي في `resetServer.php` كان بيعمل هاش لكلمة `"12345"` (5 أرقام) مش `"123456"` زي ما التقرير الأصلي افترض — يعني كان فيه عدم تطابق أصلاً بين القيمة الحقيقية المخزّنة والقيمة المعروضة للأدمن بعد الـ reset. المشكلتين اتصلحوا مع بعض:

- `Admin/Templates/resetServer.php`: بقى بيولّد كلمة مرور عشوائية فعليًا (`random_bytes(6)` → 12 حرف hex) لكل عملية Server Reset، ويعمل لها bcrypt hash زي ما كان، ويحفظ النسخة الصريحة في `$_SESSION` (مش في الرابط/URL) عشان تتعرض مرة واحدة بس.
- `Admin/Templates/resetdone.php`: بقى بيقرأ كلمة المرور الحقيقية من الـ session ويمسحها فورًا بعد القراءة (one-time reveal) — بدل القيمة الثابتة القديمة.

**الحالة: مغلق. كل عملية Server Reset دلوقتي بتولّد كلمة مرور عشوائية حقيقية.**

---

### 🟠 البند العالي #8 — `docker-compose.yml` منافذ مكشوفة — **تم تخفيف الخطر (مش حذف كامل، بقرار واعي)**

- منفذ MariaDB (3306) ومنفذ phpMyAdmin (8081) كانوا متاحين على كل الشبكة (`0.0.0.0`) — اتغيّروا ليبقوا مربوطين بـ `127.0.0.1` بس (الجهاز المحلي فقط). ده بيقفل خطر التعرض للإنترنت/الشبكة بشكل افتراضي، وبرضه بيسيب أدوات التطوير المحلية شغالة زي ما هي.
- **قرار مقصود بعدم الحذف الكامل:** الـ fallback الضعيف لكلمة مرور `rootpassword` لو الـ `.env` مش موجود **اتسابت عمدًا** (بس بتحذير واضح مكتوب فوقها في الملف) بدل ما تتشال بالكامل، لأن شيلها كان ممكن يكسر `docker compose up` لأي حد (يشمل عمر نفسه) مالوش `.env` جاهز دلوقتي. القرار ده يستاهل مراجعة بشرية صريحة قبل أي نشر إنتاجي فعلي.

**الحالة: الخطر الأساسي (تعرّض شبكي خارجي) اتقفل. الاعتماد على fallback ضعيف لسه موجود بس محذّر منه بوضوح، وده قرار للعميل يحسمه قبل production.**

---

### 🟠 البند العالي #6 — `AC_OETags.js` — **تم الحذف، بعد تأكيد فعلي**

- عملت `grep` شامل على المشروع بالكامل قبل الحذف (مش افتراض) — اتأكد إن الملف **مفيهوش أي مرجع واحد** (لا `<script src>`, لا include, لا حتى ذكر لاسمه في أي مكان تاني).
- الملف اتحذف نهائيًا.

**الحالة: مغلق.**

---

### 🟡 البند المتوسط #9 — أسماء ملفات فيها "zravianx" — **تم الحل**

- `gpack/novaterra_t4/images/footer_logo_zravianx.gif` → `footer_logo_novaterra.gif`
- `gpack/novaterra_t4/images/banner-zravianx.png` → `banner-novaterra.png`
- اتأكد قبل إعادة التسمية إن الملفين مالهومش أي مرجع في أي `.tpl`/`.php` حي (كانوا بس متوثقين في ملفات status.md قديمة) — فمفيش خطر كسر layout.

**الحالة: مغلق.**

---

## 🔴 لسه لم يُلمس (بعد الدفعة التانية)

### عالية الأولوية
4. **صور فن الأبطال تشبه Travian الرسمي** — لسه محتاجة فحص بصري + قرار بشري/قانوني. لم تُفتح أي صورة في الجلستين.
5. **شعارات PayPal/Skrill الحقيقية في `img/bezahlung/`** — لسه موجودة كما هي.

### متوسطة الأولوية
10. ~~بريد شخصي في ترويسات~~ — **تم الحل بالكامل في الدفعة دي** (كان البند دا هو التركيز الأساسي).
11. رابط GitHub placeholder `https://github.com/YOUR-ORG/Novaterra` — لسه موجود في عشرات الملفات (شفناه أثناء الفحص بس محتاج الرابط الحقيقي من صاحب المشروع قبل التصحيح الجماعي).
12. فولدر `installed_1786718416/` — **ملاحظة جديدة:** اتأكد إنه بالفعل نسخة طبق الأصل تقريبًا من `install/` (نفس ملف `end.tpl` بالظبط اتكرر فيه نفس المشكلة) — يبدو نسخة احتياطية/تجريبية قديمة. قرار الحذف أو الدمج لسه محتاج توضيح من صاحب المشروع.
13. `var/installed` — اتفحص، مش موجود في نسخة التسليم الحالية.
14. ~380 صورة خارج `gpack/` — لسه محتاجة فحص بصري كامل.
15. `img/lol.PNG` مستخدمة في `maintenance.php` بدل `img/maintenance.png` — لسه لم يُصلح (تبديل بسيط جدًا، مؤجل فقط لأولوية أقل).

---

## الأولوية المقترحة للجلسة الجاية (محدّثة)

1. **البنود العالية المتبقية (#4، #5):** الصور — أبطأ شغل، يحتاج فحص بصري فعلي لكل صورة وربما قرار بشري نهائي (استبدال فني/ترخيص/استشارة قانونية).
2. **`img/lol.PNG` → `img/maintenance.png` في `maintenance.php`:** تصليح سريع جدًا، خطوة تالية منطقية بسيطة.
3. **رابط GitHub placeholder (بند #11):** يحتاج بس تأكيد الرابط الحقيقي من صاحب المشروع، بعدين استبدال جماعي سريع بنفس أسلوب باقي الـ header fixes.
4. **`installed_1786718416/` (بند #12):** يحتاج سؤال مباشر لصاحب المشروع: نسخة احتياطية يتم حذفها، ولا لها غرض؟
5. **مراجعة قرار docker-compose.yml النهائي:** هل نحذف الـ fallback الضعيف بالكامل (breaking change محتاج `.env` جاهز)، ولا نسيبه بالتحذير الحالي؟
